% Slide 6 -- Pipeline overview
\begin{frame}{Pipeline overview}
  \begin{center}
  \begin{tikzpicture}[
    scale=0.85, transform shape,
    node distance=0.4cm and 0.45cm,
    box/.style={
      rectangle, rounded corners=2pt, draw=medBlue, thick, fill=medSoft,
      text width=2.2cm, align=center, minimum height=0.95cm, font=\scriptsize
    },
    feat/.style={
      rectangle, rounded corners=2pt, draw=medTeal, thick, fill=medTeal!15,
      text width=2.4cm, align=center, minimum height=0.95cm, font=\scriptsize
    },
    arr/.style={->, thick, medBlue, >=Stealth}
  ]
    \node[box] (dicom) {DICOM\\axial fs MRI};
    \node[box, right=of dicom] (sam) {SAM seed\\(central slice)};
    \node[box, right=of sam] (prop) {Bidirectional\\propagation};
    \node[box, right=of prop] (post) {Post-processing\\(neighbor + ring)};

    \node[box, below=1.0cm of sam] (val) {Area-curve\\validation\\(direction-aware)};
    \node[box, right=of val] (resam) {Optional SAM\\re-segmentation};

    \node[feat, below=1.0cm of val] (circles) {Per-slice\\articular circles};
    \node[feat, right=of circles] (sphere) {Approximating\\sphere};
    \node[feat, right=of sphere] (locate) {Tear localization\\(rotator cuff)};

    \draw[arr] (dicom) -- (sam);
    \draw[arr] (sam) -- (prop);
    \draw[arr] (prop) -- (post);
    \draw[arr] (post.south) |- (val.east);
    \draw[arr] (val) -- (resam);
    \draw[arr] (val.south) |- (circles.north);
    \draw[arr] (resam.south) |- (sphere.north);
    \draw[arr] (circles) -- (sphere);
    \draw[arr] (sphere) -- (locate);
  \end{tikzpicture}
  \end{center}

  \vspace{0.4em}
  {\footnotesize \textcolor{medTeal}{Frozen SAM + classical geometry + clinical anatomy.}\\
  Every block produces \alert{auditable} outputs: masks, CSVs, summary figures.}
\end{frame}
