% Slide 11 -- Parameterizing the articular circle (text only)
\begin{frame}{The geometric trick: parameterizing the articular circle}
  \begin{block}{From a circle to a 1D angular map}
    Each per-slice circle has an in-plane orthonormal basis $(\mathbf{u},\mathbf{v})$ and center $\mathbf{c}$.
    Any inlier point $\mathbf{p}$ has angular coordinate
    \[
      \theta = \mathrm{atan2}\big((\mathbf{p}-\mathbf{c})\!\cdot\!\mathbf{v},\; (\mathbf{p}-\mathbf{c})\!\cdot\!\mathbf{u}\big).
    \]
  \end{block}

  \begin{block}{Why this is powerful}
    The articular circle becomes a \alert{1D tendon map}: each $\theta$ on the circle corresponds to a known anatomical direction at the humeral head, the same for every patient and every vendor.
  \end{block}
\end{frame}

% Slide 11b -- Parameterization diagram (image alone)
\begin{frame}{Angular parameterization of the circle}
  \centering
  \begin{tikzpicture}[scale=1.2]
    \filldraw[medSoft] (0,0) circle (2.0);
    \draw[thick, medBlue] (0,0) circle (2.0);
    \draw[->] (-2.6,0) -- (2.6,0) node[right, font=\footnotesize] {$\mathbf{u}$};
    \draw[->] (0,-2.6) -- (0,2.6) node[above, font=\footnotesize] {$\mathbf{v}$};
    \filldraw[medBlue] (0,0) circle (1.8pt);
    \node[medBlue, font=\footnotesize, below left] at (0,0) {$\mathbf{c}$};
    \filldraw[medAccent] (60:2.0) circle (2.5pt);
    \draw[medAccent, dashed] (0,0) -- (60:2.0);
    \node[medAccent, font=\footnotesize, above right] at (60:2.0) {$\mathbf{p}$};
    \draw[medAccent] (0.6,0) arc (0:60:0.6);
    \node[medAccent, font=\footnotesize] at (35:0.85) {$\theta$};
  \end{tikzpicture}
  \\[0.6em]
  {\footnotesize $\theta$ is the angular coordinate of every inlier on the articular circle.}
\end{frame}

% Slide 11c -- Anatomical sector mapping (text only)
\begin{frame}{Angular sectors $\to$ rotator-cuff tendons}
  \begin{block}{Anatomical sector mapping}
    Tendon insertions fall in \alert{predictable angular sectors} on the articular circle:
    \begin{itemize}
      \item \textbf{Subscapularis} $\to$ lesser tubercle: $\sim 0$--$45^\circ$.
      \item \textbf{Supraspinatus} $\to$ greater tub.\ (sup): $\sim 45$--$135^\circ$.
      \item \textbf{Infraspinatus} $\to$ greater tub.\ (post): $\sim 135$--$225^\circ$.
      \item \textbf{Teres minor} $\to$ greater tub.\ (inf-post): $\sim 225$--$270^\circ$.
    \end{itemize}
  \end{block}

  \begin{block}{Localization rule}
    A focal drop in inlier ratio in a specific sector $\to$ \alert{candidate tear} in the corresponding tendon, at the slice height where the drop occurs.
  \end{block}
\end{frame}

% Slide 11d -- Sector pie diagram (image alone)
\begin{frame}{Sector diagram: tendon insertions and a focal drop}
  \centering
  \begin{tikzpicture}[scale=1.3]
    \filldraw[medSoft] (0,0) circle (2.2);
    \draw[thick, medBlue] (0,0) circle (2.2);
    \filldraw[medTeal!30] (0,0) -- (2.2,0) arc (0:45:2.2) -- cycle;
    \filldraw[medTeal!50] (0,0) -- (45:2.2) arc (45:135:2.2) -- cycle;
    \filldraw[medTeal!30] (0,0) -- (135:2.2) arc (135:225:2.2) -- cycle;
    \filldraw[medTeal!50] (0,0) -- (225:2.2) arc (225:270:2.2) -- cycle;
    \node[font=\footnotesize] at (22:1.5) {Subscap.};
    \node[font=\footnotesize] at (90:1.5) {Supraspin.};
    \node[font=\footnotesize] at (180:1.5) {Infraspin.};
    \node[font=\footnotesize] at (247:1.5) {T. minor};
    \draw[medAccent, ultra thick] (60:2.0) -- (60:2.6);
    \draw[medAccent, ultra thick] (75:2.6) -- (75:2.0);
    \node[medAccent, font=\footnotesize, above] at (67:2.8) {focal drop};
  \end{tikzpicture}
  \\[0.6em]
  {\footnotesize The articular circle becomes a 1D \emph{tendon map}; a focal drop in inlier ratio identifies the affected tendon.}
\end{frame}
